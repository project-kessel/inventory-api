schema_version: "1.0"
name: user
description: "User/principal for ownership relations"

common:
  # Empty schema - no relations
  schema:
    type: object
    properties: {}

  # No relations for user
  relations: []

reporters:
  - name: rbac
    description: "RBAC service reporter for user management"
